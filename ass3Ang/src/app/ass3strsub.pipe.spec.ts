import { Ass3strsubPipe } from './ass3strsub.pipe';

describe('Ass3strsubPipe', () => {
  it('create an instance', () => {
    const pipe = new Ass3strsubPipe();
    expect(pipe).toBeTruthy();
  });
});
